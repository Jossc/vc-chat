server:
  port: 8888

spring:
  profiles:
    include: native
  application:
    name: vc-chat-configserver
  cloud:
    config:
      server:
        native:
          search-locations: classpath:config
management:
  security:
    enabled: false